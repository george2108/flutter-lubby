import 'package:flutter/widgets.dart';

const kShapeModalBottom = RoundedRectangleBorder(
  borderRadius: BorderRadius.horizontal(
    left: Radius.circular(20),
    right: Radius.circular(20),
  ),
);
