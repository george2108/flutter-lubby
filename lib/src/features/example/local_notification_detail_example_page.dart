import 'package:flutter/material.dart';

class LocalNotificationDetailExamplePage extends StatelessWidget {
  const LocalNotificationDetailExamplePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('detalle de notificacion'),
      ),
    );
  }
}
