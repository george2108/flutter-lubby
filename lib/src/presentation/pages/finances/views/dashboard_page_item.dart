part of '../finances_main_page.dart';

class DashboardPageItem extends StatelessWidget {
  const DashboardPageItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('dashboard'),
    );
  }
}
