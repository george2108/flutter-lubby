part of '../finances_page.dart';

class SettingsPageItemWidget extends StatelessWidget {
  const SettingsPageItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Configuración'),
    );
  }
}
