part of '../finances_page.dart';

class AccountsPageItemWidget extends StatelessWidget {
  const AccountsPageItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('cuentas'),
    );
  }
}
