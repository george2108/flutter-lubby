part of '../finances_page.dart';

class DashboardPageItemWidget extends StatelessWidget {
  const DashboardPageItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('dashboard'),
    );
  }
}
