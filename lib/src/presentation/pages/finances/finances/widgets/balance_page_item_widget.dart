part of '../finances_page.dart';

class BalancePageItemWidget extends StatelessWidget {
  const BalancePageItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('balance'),
    );
  }
}
