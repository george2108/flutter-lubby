part of '../reminders_main_page.dart';

class CustomRemindersPageItem extends StatelessWidget {
  const CustomRemindersPageItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Personalizados'),
    );
  }
}
