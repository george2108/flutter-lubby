import 'package:flutter/material.dart';

class NotesHelpPage extends StatelessWidget {
  const NotesHelpPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PageView(
        children: [],
      ),
    );
  }
}
